package ai.hypergraph.kotlingrad.typelevel.chinese

import kotlin.test.*

/*
./gradlew :kotlingrad:jvmTest --tests "ai.hypergraph.kotlingrad.typelevel.chinese.算数测试"
*/
@Suppress("ClassName", "NonAsciiCharacters")
class 算盘测试 {
  @Test
  fun 算数测试() {
//    val 二十一 = 十七 加 四
//
//    val 四十二 = 十七
//      .let { it 加 二 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 一 }
//      .let { it 加 三 }
//
//    assertEquals(42, 四十二.toInt())
  }
}